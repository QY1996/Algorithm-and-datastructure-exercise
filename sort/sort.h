#include <vector>
#include <iostream>

void show(std::vector<int>& arr);

void bubble_sort(std::vector<int>& arr, bool verbose = false);
void bucket_sort(std::vector<int>& arr, bool verbose = false);
void counting_sort(std::vector<int>& arr, bool verbose = false);
void heap_sort(std::vector<int>& arr, bool verbose = false);
void insertion_sort(std::vector<int>& arr, bool verbose = false);
void merge_sort(std::vector<int>& arr, bool verbose = false);
void quick_sort(std::vector<int>& arr, bool verbose = false);
void radix_sort(std::vector<int>& arr, bool verbose = false);
void selection_sort(std::vector<int>& arr, bool verbose = false);
void shell_sort(std::vector<int>& arr, bool verbose = false);